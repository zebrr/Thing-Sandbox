# TS-AUDIT-001: Project Inventory and Structure Mapping

## References
- `docs/Thing' Sandbox Architecture.md` — архитектурное описание проекта
- `docs/specs/` — все спецификации модулей
- `src/` — исходный код
- `tests/` — тесты

## Context
Проводим аудит проекта для выявления рассинхронизации между документацией, кодом и тестами. Первый шаг — построить полную карту проекта и найти "сирот".

## Steps

### 1. Инвентаризация модулей
Для каждого Python-модуля в `src/` (включая вложенные `src/utils/`, `src/phases/`, `src/utils/llm_adapters/`) составить таблицу:

| Module | Spec exists? | Spec file | Unit tests? | Integration tests? |
|--------|--------------|-----------|-------------|-------------------|

### 2. Проверка Architecture.md
- Прочитать `docs/Thing' Sandbox Architecture.md`
- Выписать все модули/компоненты, которые там описаны
- Проверить: каждый описанный модуль существует в `src/`?
- Проверить: есть ли в `src/` модули, НЕ описанные в Architecture?

### 3. Проверка спецификаций
- Для каждого файла в `docs/specs/`:
  - Определить, какой модуль он описывает
  - Проверить: существует ли этот модуль в `src/`?
- Найти спеки без соответствующего кода

### 4. Проверка тестов
- Для каждого тестового файла в `tests/unit/` и `tests/integration/`:
  - Определить, какой модуль он тестирует
  - Проверить: существует ли этот модуль?
- Найти модули без тестов
- Найти тесты без соответствующих модулей

### 5. JSON-схемы
- Перечислить все схемы в `src/schemas/`
- Для каждой: используется ли она где-то в коде?

## Testing
Задача аналитическая, автоматического тестирования не требуется.

## Deliverables

### Файл отчёта: `docs/tasks/TS-AUDIT-001_REPORT.md`

Формат отчёта:

```markdown
# Audit Report: Project Inventory and Structure

## Summary
[Краткий обзор: сколько модулей, спек, тестов; общее состояние]

## Module Inventory Table
[Полная таблица модулей]

## Architecture Coverage
### Modules in Architecture but missing in code:
### Modules in code but missing in Architecture:

## Specs Coverage  
### Specs without corresponding code:
### Modules without specs:

## Tests Coverage
### Modules without unit tests:
### Modules without integration tests:
### Test files without corresponding modules:

## JSON Schemas
### Schemas and their usage:

## ✅ OK
[Что в порядке]

## ⚠️ Warnings
[Несоответствия, требующие внимания]

## ❌ Issues
[Критичные проблемы]

## 📋 Recommendations
[Конкретные действия для следующих шагов аудита]
```
