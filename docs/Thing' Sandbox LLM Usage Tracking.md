# Thing' Sandbox: LLM Usage Tracking

Документ описывает сбор, хранение и отображение статистики использования LLM.

---

## 1. Метрики

### Собираемые поля

| Поле | Источник API | Описание |
|------|--------------|----------|
| `total_tokens` | `usage.total_tokens` | Сумма input + output токенов (основная метрика биллинга) |
| `reasoning_tokens` | `usage.output_tokens_details.reasoning_tokens` | Токены на reasoning (подмножество output) |
| `cached_tokens` | `usage.input_tokens_details.cached_tokens` | Токены из кэша OpenAI |
| `total_requests` | счётчик | Количество API вызовов |

### Примечания

- `total_tokens` — главная метрика, за неё платим
- `reasoning_tokens` — значительная часть для o-моделей (до 70% output)
- `cached_tokens` — влияет на стоимость, полезен для анализа эффективности промптов

---

## 2. Хранение

Статистика хранится накопительным итогом — обновляется каждый такт.

### 2.1. Агрегат симуляции

Файл: `simulation.json`

```json
{
  "id": "demo-sim",
  "current_tick": 42,
  "status": "paused",
  "_openai": {
    "total_tokens": 1250000,
    "reasoning_tokens": 450000,
    "cached_tokens": 320000,
    "total_requests": 420
  }
}
```

### 2.2. По персонажам

Файл: `characters/{id}.json`

```json
{
  "identity": {...},
  "state": {...},
  "memory": {...},
  "_openai": {
    "intention_chain": ["resp_abc123"],
    "memory_chain": ["resp_xyz789"],
    "usage": {
      "total_tokens": 125000,
      "reasoning_tokens": 45000,
      "cached_tokens": 32000,
      "total_requests": 42
    }
  }
}
```

### 2.3. По локациям

Файл: `locations/{id}.json`

```json
{
  "identity": {...},
  "state": {...},
  "_openai": {
    "resolution_chain": ["resp_111"],
    "narrative_chain": ["resp_222"],
    "usage": {
      "total_tokens": 340000,
      "reasoning_tokens": 120000,
      "cached_tokens": 85000,
      "total_requests": 84
    }
  }
}
```

### 2.4. Namespace `_openai`

- Префикс `_` указывает на служебные данные
- Позволяет добавить другие провайдеры (`_anthropic`, `_openrouter`) без конфликтов
- Содержит как chains (для response chaining), так и usage (статистика)

---

## 3. Логирование в консоль

### 3.1. По фазам (уровень INFO)

После завершения каждой фазы:

```
2025.06.05 14:32:10 | INFO    | 🎭 phase1: Complete (2 chars, 4,500 tokens, 1,200 reasoning)
2025.06.05 14:32:12 | INFO    | ⚖️ phase2a: Complete (3 locs, 12,000 tokens, 4,500 reasoning)
2025.06.05 14:32:15 | INFO    | 📖 phase2b: Complete (3 locs, 8,200 tokens, 0 reasoning)
2025.06.05 14:32:18 | INFO    | 🧠 phase4: Complete (2 chars, 3,100 tokens, 800 reasoning)
```

### 3.2. Итог такта (уровень INFO)

После завершения такта:

```
2025.06.05 14:32:18 | INFO    | 🎬 runner: Tick 42 complete (8.2s, 27,800 tokens, 6,500 reasoning)
```

### 3.3. Формат

- Числа с разделителем тысяч: `27,800`
- `reasoning` показывается как дополнительная информация, не отдельно
- `cached_tokens` в консоль не выводится (только в файлы)

---

## 4. Reasoning Summary (опционально)

### 4.1. Описание

OpenAI возвращает `reasoning.summary` для o-моделей — краткое описание хода рассуждений. Полезно для отладки и анализа поведения персонажей.

### 4.2. Получение

- Адаптер извлекает из `output[0].summary` (для reasoning моделей)
- Возвращается в `AdapterResponse.debug.reasoning_summary: list[str] | None`
- Доступно в памяти во время выполнения такта

### 4.3. Сохранение (будущее)

- **По умолчанию:** не сохраняется
- **По опции:** `--save-reasoning` или `config.runner.save_reasoning_summary = true`
- **Формат:** определяется при реализации

---

## 5. Поток данных

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. load_simulation()                                            │
│    Simulation загружается с накопленной статистикой             │
└─────────────────────────────┬───────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 2. Runner создаёт entity dicts через model_dump()               │
│    - char_entities для Phase 1, Phase 4                         │
│    - loc_entities для Phase 2a, Phase 2b                        │
└─────────────────────────────┬───────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 3. Фазы выполняются                                             │
│    LLMClient мутирует entity dicts:                             │
│    - Добавляет response_id в chains                             │
│    - Накапливает usage статистику                               │
│    Runner логирует статистику после каждой фазы                 │
└─────────────────────────────┬───────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 4. Синхронизация после фаз                                      │
│    Runner копирует _openai из entity dicts обратно в Simulation │
└─────────────────────────────┬───────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 5. Агрегация                                                    │
│    Runner суммирует usage из всех entities в simulation._openai │
└─────────────────────────────┬───────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 6. save_simulation()                                            │
│    Атомарно сохраняет всё на диск                               │
│    Runner логирует итоговую статистику такта                    │
└─────────────────────────────────────────────────────────────────┘
```

---

## 6. Связанные документы

- [LLM Approach v2](Thing'%20Sandbox%20LLM%20Approach%20v2.md) — архитектура LLM клиента и адаптеров
- [OpenAI Responses API Reference](Thing'%20Sandbox%20OpenAI%20Responses%20API%20Reference.md) — формат usage в API
- [Architecture](Thing'%20Sandbox%20Architecture.md) — общая архитектура проекта
